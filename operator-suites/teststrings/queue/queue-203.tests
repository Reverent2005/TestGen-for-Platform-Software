test QueueGenerated203 {
    sequence: isEmpty -> enqueue -> enqueue -> dequeue -> front -> dequeue -> front;

    inputs {
        enqueue[1].elem = "gamma";
        enqueue[2].elem = "alpha";
    }
}
