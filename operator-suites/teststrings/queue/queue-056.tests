test QueueGenerated56 {
    sequence: enqueue -> enqueue -> dequeue -> front -> front -> dequeue -> isEmpty;

    inputs {
        enqueue[0].elem = "beta";
        enqueue[1].elem = "gamma";
    }
}
