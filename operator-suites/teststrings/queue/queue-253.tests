test QueueGenerated253 {
    sequence: enqueue -> enqueue -> enqueue -> dequeue -> front -> isEmpty;

    inputs {
        enqueue[0].elem = "gamma";
        enqueue[1].elem = "alpha";
        enqueue[2].elem = "beta";
    }
}
