test QueueGenerated111 {
    sequence: enqueue -> isEmpty -> enqueue -> dequeue -> dequeue -> front -> dequeue;

    inputs {
        enqueue[0].elem = "second";
        enqueue[2].elem = "alpha";
    }
}
