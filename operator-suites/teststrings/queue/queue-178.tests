test QueueGenerated178 {
    sequence: enqueue -> front -> enqueue -> dequeue -> front -> front -> isEmpty;

    inputs {
        enqueue[0].elem = "gamma";
        enqueue[2].elem = "gamma";
    }
}
