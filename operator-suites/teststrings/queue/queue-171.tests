test QueueGenerated171 {
    sequence: enqueue -> enqueue -> dequeue -> dequeue -> front -> front -> isEmpty;

    inputs {
        enqueue[0].elem = "first";
        enqueue[1].elem = "first";
    }
}
