test StackGenerated196 {
    sequence: push -> push -> pop -> push -> peek -> peek;

    inputs {
        push[0].elem = "alpha";
        push[1].elem = "alpha";
        push[3].elem = "beta";
    }
}
